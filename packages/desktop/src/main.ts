// Desktop entry point — launches as a VSIX extension via Code-OSS or packaged Electron app.
// For MVP, the VSIX is the primary distribution. Electron packaging via electron-builder is configured but optional.
console.log('lambda128 Desktop — use the VSIX extension in VS Code / VS Codium');